/**
 * Compat — Finance Engine vive em `src/engine/finance/`.
 * Reexporta a API usada pela Career Engine.
 */

export {
  resolveWeeklyFinance,
  processWeeklyFinance,
  trySignSponsorship,
  yearlyToWeekly,
} from '../finance'
